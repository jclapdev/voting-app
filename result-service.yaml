apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: NodePort # Needs to be accessible to the outside world
  ports:
    - name: "result-service"
      port: 8081
      targetPort: 80
      nodePort: 31001
  selector:
    app: result
