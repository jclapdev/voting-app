apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
spec:
  type: ClusterIP # needs to be accessible only to other nodes in cluster
  ports:
    - name: "db-service"
      port: 5432
      targetPort: 5432
  selector:
    app: db
